from fastapi import FastAPI\nimport numpy as np\n\napp = FastAPI()\n\n@app.get("/ipsa")\ndef calculate_ipsa():\n    # Logic for IPSA calculations goes here\n    return {"result": "IPSA calculation result"}\n\n@app.get("/montecarlo")\ndef monte_carlo_simulation():\n    # Logic for Monte Carlo simulation goes here\n    iterations = 10000\n    results = []\n    for _ in range(iterations):\n        # Simulate something and store the result\n        results.append(np.random.rand())\n    mean_result = np.mean(results)\n    return {"mean": mean_result}